Items for this:

- Need a Google Drive backup option
- holla command needs to be able to:
  - backup full labels at once
  - Capital A Archicve to... command doesn't work, it pops up nothing and doesn't let me select a location.
- holla has a visual bug where when I delete down to one item in a label,
  the item doesn't show as selected. It still is selected as it shows the
  preview and lets me delete it, but it doesn't appear that way and is
  confusing
- MCP needs to be opt-in per feature with serious privacy controls/opt-outs
